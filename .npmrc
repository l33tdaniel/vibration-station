# Supply-chain hardening: only install dependency versions that have been
# published for more than 7 days. Blocks freshly-published (potentially
# compromised) releases. Requires npm >= 11.6. The committed package-lock.json
# is generated under this floor, so `npm install` is both reproducible and aged.
min-release-age=7
